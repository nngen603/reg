.env
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/
.DS_Store

# Working output directory. Regenerated by every run.
# Representative results are committed under samples/ instead, so a reviewer
# can inspect real output without installing anything or holding an API key.
outputs/
